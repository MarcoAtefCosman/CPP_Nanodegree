#include <iostream>
#include <vector>

using namespace std;

int main(){

    // //if and logical operators
    // bool accept = true;
    // bool reject = false;

    // int x = 0;
    // int y = 1;

    // cout << "using bool vlaues !"<<endl;

    // if (accept) {cout << "accepted condition using accept !"<<endl;}

    // if (accept && reject) {cout << "accepted conditon using and operator !"<<endl; }

    // if (accept || reject) {cout << "accepted condition using or operator !"<<endl;}

    // if (!reject) {cout  << "accepted conditon using not operator !"<<endl;}

    // cout << "using numeirc vlaues !"<<endl;
    
    // if (y) {cout << "accepted condition using accept !"<<endl;}

    // if (x && y) {cout << "accepted conditon using and operator !"<<endl; }

    // if (x || y) {cout << "accepted condition using or operator !"<<endl;}

    // if (!x) {cout  << "accepted conditon using not operator !"<<endl;}

    // //while loops
    // auto i = 0;
    // while (i < 5){
    //     cout << i << endl;
    //     i++;
    // }

//     auto i = 1;
//     while (i <= 10){
//         if (i%2 == 0){cout << i << " is even number"<<endl;}
//         i++;
//     }
// }
